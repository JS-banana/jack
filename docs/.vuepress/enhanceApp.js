import ViewUI from '../../examples/index'
import components from '../../examples/components'

export default ({
  Vue,
  // options,
  // router,
  // siteData,
}) => {
  Vue.use(ViewUI)
  Vue.use(components)
}
