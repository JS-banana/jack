// import * as utils from '@ah-ailpha/utils'
// import { randomColor } from '@ah-ailpha/utils' // 按需加载
import { formatPassTime, formatRemainTime, randomColor } from '@ah-ailpha/utils'

console.log('🎉工具方法↓↓↓↓↓')
console.log('@ah-ailpha/utils：randomColor', randomColor())
console.log('\n')

export { formatPassTime, formatRemainTime, randomColor }
